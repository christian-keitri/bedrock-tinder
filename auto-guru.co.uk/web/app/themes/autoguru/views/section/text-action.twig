{#
<div class="w-full h-auto pb-20 bg-secondary flex flex-col justify-left items-left px-40">
  <h1 class="text-5xl font-medium">{{ title }}</h1>
  <p class="text-base text-grey mt-4 px-0">{{ subtitle }}</p>
  <div class="mt-8">
    {% include 'partial/buttons/primary-button.twig' with { text: buttonText, url: '/' } %} 
  </div>
</div>
#}

<div class="w-full h-auto pb-20 bg-secondary flex flex-col justify-center items-center px-4 sm:px-40 text-center">
  <div class="bg-white w-full max-w-4xl px-10 py-16 rounded-3xl shadow-lg shadow-black/10 ring-1 ring-white/30">
    <div class="shadow-inner rounded-3xl px-6 py-6">
      <h1 class="text-5xl font-medium text-gray-900">{{ title }}</h1>
      <p class="text-base text-gray-600 mt-4">{{ subtitle }}</p>
      <div class="mt-8">
        {% include 'partial/buttons/primary-button.twig' with { text: buttonText, url: '/' } %}
      </div>
    </div>
  </div>
</div>
