{# <div class="bg-cover bg-center bg-no-repeat w-full h-200 text-center text-white flex flex-col justify-center items-center" style="background-image: url('{{ image }}');"> #}
<div class="mt-10 bg-cover bg-center bg-no-repeat w-full h-200 text-center text-white flex flex-col justify-center items-center px-4 sm:px-0" style="background-image: url('{{ image }}');">
  {# <h1 class="text-5xl font-bold">{{ title }}</h1> #}
  <h1 class="text-3xl sm:text-4xl md:text-5xl leading-none sm:leading-[normal] font-bold px-5 sm:px-10">{{ title }}</h1>
  <p class="text-base mt-4 px-0">{{ subtitle1 }}</p>
  <p class="text-base mt-0 px-0">{{ subtitle2 }}</p>
  <div class="mt-8">
    {% include 'partial/buttons/primary-button.twig' with { text: buttonText, url: '/' } %} 
  </div>
</div>